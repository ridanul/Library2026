# Copy this file to Backend/.env and fill in values for SMTP in production/dev.
# Do NOT commit Backend/.env (it's listed in .gitignore).

# SMTP settings
SMTP_HOST=smtp.zoho.com
SMTP_PORT=465
SMTP_USER=no-reply@has-ib.dev
SMTP_PASS=@wndAjy9j3hT4gv
SMTP_FROM=no-reply@has-ib.dev

# Optional: secret key for JWT (set in production)
# SECRET_KEY=change-me-to-a-secure-random-value
